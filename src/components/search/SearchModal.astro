---
import Search from "astro-pagefind/components/Search";
import "../../styles/searchModal.css"
---

<div class="myogoo-modal-window">
	<div class="myogoo-modal-content">
		<Search className="myogoo-search" />
	</div>

	<script>
		const modalWindowClassName = "myogoo-modal-window";
		const modalBlurClassName = "modal-blur-overlay";
		document.addEventListener("keydown", (e) => {
			//show modal
			if ((e.ctrlKey || e.metaKey) && e.key.toLowerCase() === "k") {
				e.preventDefault();
				showModal();
			}
			//close modal
			if (e.key === "Escape") {
				closeModal();
			}
		});

		const modal_container = document.querySelector(".myogoo-modal-content");
		const modal = document.querySelector(`.${modalWindowClassName}`);
		const blurOverlay = document.querySelector(`.${modalBlurClassName}`);
        // 이름 추천
		const html = document.querySelector("html");

		modal?.addEventListener("click", (e) => {
			e.preventDefault();
			//closeModal()
		});

		modal_container?.addEventListener("click", (e) => {
			e.stopPropagation();
		});

		function showModal() {
			const modal = document.querySelector(`.${modalWindowClassName}`);
			modal?.setAttribute("modal-show", "true");

			const input = document.querySelector(
				".pagefind-ui__search-input"
			) as HTMLInputElement;
			input?.focus();

			blurOverlay?.setAttribute("blur","true")
			html?.setAttribute("style", "overflow:hidden");
		}

		function closeModal() {
			const modal = document.querySelector(`.${modalWindowClassName}`);
			modal?.setAttribute("modal-show", "false");

			blurOverlay?.setAttribute("blur","false")
			html?.setAttribute("style", "overflow:auto");
		}
	</script>
</div>
